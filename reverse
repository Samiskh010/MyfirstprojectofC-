#include <iostream>
#include <conio.h>
#include <math.h>
using namespace std;
void f(int a)
{
	if(a<10)
	cout<<a;
	else
	{
		cout<<a%10;
		f(a/10);
	}
}
int main()
{
	int a;
	cout<<"enter a please :";
	cin>>a;
	f(a);
}
